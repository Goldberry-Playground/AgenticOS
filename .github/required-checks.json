{
  "//": "Declared required status checks for this repo (GOL-1735 / GOL-1406-D).",
  "//audit": "audit-required-merge-group.py asserts every context below is produced by a workflow job whose `on:` includes `merge_group`, so no required check is PR-only and the merge queue cannot wedge ('Expected — waiting for status'). The weekly reconcile job compares this list against live branch protection and fails on drift, so this file must be kept in sync when a required check is added or removed.",
  "//gol1953-pause": "Promotion of the guard layer to required checks (GOL-1953) is PAUSED per board 2026-09-01. This list is kept EQUAL to live branch protection so the reconcile leg stays green; the promoted-target set lives in .github/merge-policy.json under required_contexts. The promotion will be re-declared here per-repo IN LOCKSTEP with `apply-merge-policy.sh --apply` (Josh) after (a) GOL-1958 missing-check sweep lands and (b) the `Protected paths guard` merge_group-enforcement question is resolved (it is pass-through on merge_group today, so requiring it as-is gates nothing — tracked separately). Until then declared == live.",
  "branch": "main",
  "required_contexts": [
    "Lint",
    "Typecheck",
    "Unit tests",
    "Build"
  ]
}
